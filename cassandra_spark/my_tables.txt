CREATE TABLE trading.daily_quotes (
    symbol text,
    date int,
    adj_close float,
    close float,
    high float,
    low float,
    open float,
    split_factor float,
    vol bigint,
    PRIMARY KEY (symbol, date)
) WITH CLUSTERING ORDER BY (date DESC)

